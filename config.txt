# 3DS Zip Extractor - Configuration File
#
# This file supports both settings and URLs
# Lines starting with # are comments and will be ignored
# Empty lines are also ignored

# ===== SETTINGS =====
# Extract path (where files will be extracted)
extract_path=sdmc:/extracted/

# Auto-retry failed downloads (true/false)
auto_retry=true

# Maximum retry attempts (0-10)
max_retries=3

# ===== DOWNLOAD URLS =====
# Add one URL per line below
# Supports:
# - Direct HTTP/HTTPS URLs
# - Google Drive URLs
# - Multiple archive formats (ZIP, TAR, 7Z, etc.)

# Example: Direct download URLs
# https://example.com/archive1.zip
# https://example.com/archive2.tar.gz

# Example: Google Drive URLs
# https://drive.google.com/file/d/1ABC_YOUR_FILE_ID_XYZ/view
# https://drive.google.com/open?id=1DEF_ANOTHER_FILE_ID_123

# Example: Test URLs (small files for testing)
# https://speedtest.tele2.net/1MB.zip
# https://speedtest.tele2.net/5MB.zip

# Add your URLs below (remove the # to uncomment):
# https://your-url-here.com/file.zip
